Coffees ordered: <br>

  All order items: <%= "#{Order.last.order_items}" %> <br>

  All order items: <%= "#{Order.last.order_items[0].coffee_id}" %>, <%= "#{Order.last.order_items[0].quantity}" %> <br>

  All order items: <%= "#{Order.last.order_items[1].coffee_id}" %>, <%= "#{Order.last.order_items[1].quantity}" %> <br>

  All order items: <%= "#{Order.last.order_items[2].coffee_id}" %>, <%= "#{Order.last.order_items[2].quantity}" %> <br>

